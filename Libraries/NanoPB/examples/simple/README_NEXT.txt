The simple_compile.bat compiles the arduino nanopb files.  Make sure to include BOTH the .h and .c files in the arduino project for it to work.

The simple_java.bat compiles the server side java file for the proto.  Include this file in the implementation for java.